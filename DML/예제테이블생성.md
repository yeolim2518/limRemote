# 예제에서 사용할 테이블

```
CREATE TABLE employees 
(
    id number(10) NOT NULL,
    name varchar2(20) NOT NULL,
    department_id number(10),
    birth_date date NOT NULL,
    salary number(10) NOT NULL,
    job_id number NOT NULL,
    CONSTRAINTS EMP_PK PRIMARY KEY(id)
);


INSERT INTO employees (id, name, department_id, birth_date, salary, job_id) VALUES (1, '한놈', 1, '1999-01-01', 4000, 1);
INSERT INTO employees (id, name, department_id, birth_date, salary, job_id) VALUES(2, '두시기', 2, '2000-01-01', 2500, 2);
INSERT INTO employees (id, name, department_id, birth_date, salary, job_id) VALUES(3, '석삼', 1, '1999-01-01', 3700, 2);
INSERT INTO employees (id, name, department_id, birth_date, salary, job_id) VALUES(4, '너구리', 2, '2000-01-01', 5000, 2);
INSERT INTO employees (id, name, department_id, birth_date, salary, job_id) VALUES(5, '오징어', 1, '2010-01-01', 5000, 2);
INSERT INTO employees (id, name, department_id, birth_date, salary, job_id) VALUES(6, '육개장', 1, '2017-01-01', 5000, 1);
INSERT INTO employees (id, name, department_id, birth_date, salary, job_id) VALUES(7, '칠뜨기', 2, '2018-01-01', 5000, 1);
INSERT INTO employees (id, name, department_id, birth_date, salary, job_id) VALUES(8, '칠뜨기', null, '2014-01-01', 5000, 1);

CREATE TABLE department
(
    id number(10) NOT NULL,
    name varchar2(20) NOT NULL,
    CONSTRAINTS DEPT_PK PRIMARY KEY(id)
);

INSERT INTO department (id, name) VALUES(1, '영업팀');
INSERT INTO department (id, name) VALUES(2, '개발팀');
INSERT INTO department (id, name) VALUES(3, '인사팀');

CREATE TABLE job
(
    id number(10) NOT NULL,
    name varchar2(20) NOT NULL,
    CONSTRAINTS JOB_PK PRIMARY KEY(id)
);

INSERT INTO job(id, name) VALUES(1, '사장');
INSERT INTO job(id, name) VALUES(2, '사원');
```